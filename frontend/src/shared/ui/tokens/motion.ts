/** Shared Framer Motion timing presets (lesson + kana). */
export const FAST_TRANSITION = { duration: 0.1, ease: "easeOut" } as const;
export const QUICK_TRANSITION = { duration: 0.12, ease: "easeOut" } as const;
